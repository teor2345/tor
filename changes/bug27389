  o Minor bugfixes (testing):
    - Allow 64-bit Windows builds to fail on Appveyor. As of 30 August
      2018, the gcc distributed by msys64 is broken. Until the underlying
      bug is fixed, allow these builds to fail.
      Fixes bug 27389; bugfix on 0.3.4.1-alpha.
